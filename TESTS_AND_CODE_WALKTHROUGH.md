# Разбор кода и тесты — лабораторные по ЭС, вариант 5

Для каждой лабы: **условие** → **разбор кода** (что и зачем) → **тест**
(команда запуска, что вводить, что должно получиться). Все тесты ниже
реально прогонялись через `tools\vip` — приведённый вывод не
теоретический, а фактический результат компиляции и запуска.

---

## Лаб.1 — основы Пролога (`labs/lab1/LAB1.PRO`)

### Условие (вариант 5)
Даны факты:
```
raspol(Предприятие, Страна)
postav(Поставщик, Покупатель, Товар, Сумма)
```
1. Программа спрашивает страну и сумму. Печатает список ВСЕХ товаров,
   которые какое-либо предприятие этой страны поставляет за границу,
   если сумма поставки ≥ указанной (с указанием поставщика,
   покупателя и страны покупателя).
2. Программа спрашивает предприятие и сумму. Если оно закупает
   за границей товар на сумму ≥ указанной — сообщить товар и
   поставщика (с его страной), иначе — «нет».
Работа должна повторяться: после ответа — вопрос «Продолжить работу?».

### Разбор кода
- **`raspol`/`postav`** — обычные факты-предикаты, база данных прямо в
  тексте программы (раздел `clauses`).
- **`repeat`** — в этом диалекте Пролога НЕ встроен, поэтому
  определён вручную (`repeat.` / `repeat:- repeat.`) — первый клоз
  даёт единственную попытку, второй — рекурсивно даёт их сколько
  угодно много при откате. На этом строится весь цикл меню.
- **`poisk1`/`poisk2`** — предикаты поиска. `poisk1` использует приём
  «вывести ВСЕ ответы»: после печати очередного результата стоит
  `fail`, что заставляет Пролог найти следующее сопоставление с
  фактами; когда варианты исчерпаны — срабатывает второй (пустой)
  клоз `poisk1(_,_)`, и предикат в целом считается успешным.
- **`poisk2`** ищет только ОДИН ответ (`!` после первого найденного) —
  и если ничего не нашлось, срабатывает второй клоз, печатающий «нет».
- **`naidi_stranu`** — вспомогательный предикат: по имени предприятия
  находит его страну (просто ищет соответствующий факт `raspol`).
- **Цикл «Продолжить работу?»** устроен так, что программа
  завершается, когда `Otvet = "нет"` (последняя цель в `repeat`-клозе
  **успешна** — тогда весь клоз завершается, откатов в `repeat`
  больше не происходит). Если ответ не «нет» — сравнение проваливается,
  происходит откат к `repeat`, и меню печатается заново. Это ключевой
  момент: если бы в конце стоял `fail` вместо условия — цикл был бы
  бесконечным (репит всегда даёт ещё одну попытку).

### Тест
```powershell
cd E:\ES2026_Labs_Variant5\labs\lab1
.\run.bat
```
Ввод:
```
1
Россия
100
нет
```
Ожидаемый вывод (проверено):
```
Товар: диод   Поставщик: Оптрон   Покупатель: IBM   Страна покупателя: США
Товар: осциллограф   Поставщик: Счетмаш   Покупатель: Оптрон   Страна покупателя: Россия
Товар: резистор   Поставщик: Кварц   Покупатель: Элтор   Страна покупателя: Польша
```
(Поставка Элтор→Счетмаш на 200 НЕ попадает в список — Элтор не в России.)

Дополнительно проверьте задание 2: ввод `2`, `Оптрон`, `400` → должно
найтись «нет» (у Оптрона нет закупок ≥400 за границей — он сам
поставщик, а не покупатель в этих данных).

---

## Лаб.2 — списки и рекурсия (`labs/lab2/LAB2.PRO`)

### Условие (вариант 5)
1. Расщепить список на два: первый — заданной длины N (первые N
   элементов), второй — остаток. Пример: `[1,5,7,3,4]` и `N=2` →
   `[1,5]` и `[7,3,4]`.
2. Даны `raspol(Страна, СписокПредприятий)` и `postav(...)`.
   Программа спрашивает страну и сумму, печатает список ВСЕХ товаров,
   поставляемых любым предприятием этой страны за границу с суммой
   ≥ указанной (поставщик, покупатель, страна покупателя).

### Разбор кода
- **Два РАЗНЫХ домена-списка**: `spisok_int = integer*` (для чисел
  задания 1) и `spisok_str = string*` (для списка предприятий задания
  2). Это принципиально: при компиляции всплыла реальная ошибка —
  изначально был один домен `string*` на оба случая, из-за чего
  `readterm` не мог прочитать список чисел `[1,5,7,3,4]` (компилятор
  ожидал строки в кавычках). Исправлено разделением доменов.
- **`rasshepit(L, N, Первая, Вторая)`** — классическая рекурсия по
  списку: если `N=0` — вся оставшаяся часть уходит во «вторую»; иначе
  голова списка уходит в «первую», а с хвостом рекурсивно вызывается
  то же самое с `N-1`.
- **`prinadl(X, L)`** — проверка принадлежности элемента списку (голова
  списка совпадает с X — успех; иначе рекурсивно ищем в хвосте).
  Используется дважды: чтобы проверить, что поставщик — предприятие
  нужной страны, и чтобы найти страну покупателя.

### Тест
```powershell
cd E:\ES2026_Labs_Variant5\labs\lab2
.\run.bat
```
Задание 1 — ввод: `1`, `[1,5,7,3,4]`, `2` →
```
Первая часть: [1,5]
Вторая часть: [7,3,4]
```
Задание 2 — ввод: `2`, `Россия`, `100` →
```
Товар: осциллограф   Поставщик: Счетмаш   Покупатель: Оптрон   Страна покупателя: Россия
Товар: диод   Поставщик: Оптрон   Покупатель: IBM   Страна покупателя: США
Товар: резистор   Поставщик: Кварц   Покупатель: Apple   Страна покупателя: США
```

---

## Лаб.3 — базы данных на Прологе (`labs/lab3/LAB3.PRO` + `lab3.dba`)

### Условие (вариант 5)
База данных предприятия:
- `contr(Шифр, Покупатель, Товар, Оплата)` — Оплата это
  **альтернативный домен**: `opl(Сумма)` (оплачено полностью) или
  `predopl(Сумма1, Сумма2)` (предоплата + доплата после поставки).
- `pokup(Предприятие, Страна, Город)`.

Нужна программа с операциями: загрузка БД, просмотр списка
контрактов, добавление нового покупателя, сохранение БД, выход
(меню).

### Разбор кода
- **`global domains` / `global facts`** — факты объявлены как
  *глобальные*, потому что они грузятся из внешнего файла `lab3.dba`
  предикатом `consult`, а не пишутся прямо в `clauses`.
- **`oplata = opl(integer); predopl(integer,integer)`** —
  альтернативный домен: аргументом `contr` может быть *любой* из этих
  двух функторов. Это позволяет одному и тому же предикату `contr`
  хранить факты с «разной формой» четвёртого аргумента.
- **`pechat_kontrakt`** — разбирает функтор по образцу (pattern
  matching): клоз с головой `pechat_kontrakt(...,opl(Summa))`
  сработает только если факт реально был создан через `opl(...)`;
  иначе Пролог перейдёт к следующему клозу с `predopl(S1,S2)`. Именно
  так на Прологе «различают» варианты альтернативного домена — не
  через `if`, а через сопоставление с образцом в самих клозах.
- **Меню (`obrab`)** — приём из методички: `repeat` в начале, и КАЖДЫЙ
  клоз `obrab`, кроме выхода (`obrab(99)`), заканчивается `fail` —
  это отправляет обратно к `repeat`, меню печатается заново.
- **`zagruzka`**: `retractall(_)` перед `consult` — убирает из памяти
  факты предыдущей загрузки (иначе при повторной загрузке они бы
  задвоились).
- **`dobav_pokup`**: `assert` создаёт факт **только в памяти** — файл
  на диске не меняется, пока не вызвано «4 — сохранение» (`save`).
  Это специально проверено тестом ниже.

### Тест
```powershell
cd E:\ES2026_Labs_Variant5\labs\lab3
.\run.bat
```
Ввод: `1`, `lab3.dba`, `2` →
```
Контракт K100: покупатель Оптрон, товар осциллограф, оплачен полностью (1000)
Контракт K200: покупатель Элтор, товар осциллограф, предоплата 400, доплата после поставки 100
Контракт K250: покупатель IBM, товар диод, предоплата 1000, доплата после поставки 0
```
Дальше ввод: `3`, `NewCorp`, `Германия`, `Берлин`, `4`, `lab3.dba`, `99`
— после этого откройте `lab3.dba` блокнотом: там должна появиться
новая строка `pokup("NewCorp","Германия","Берлин")` (проверено).

---

## Лаб.4 — логические функции (`labs/lab4/LAB4.PRO`)

### Условие (вариант 5)
Неисправности A,B,C,D сложного устройства (возможна не одна сразу):
1. если есть A или B — то C невозможна;
2. A и D не могут возникать одновременно;
3. если есть B и D одновременно — то C обычно нет;
4. по крайней мере одна неисправность есть точно;
5. неисправности A, по-видимому, нет.

Найти ВСЕ возможные комбинации неисправностей.

### Разбор кода
Каждое условие сначала переведено в булеву логику, потом — в
алгебраическую форму (по таблицам методички: дизъюнкция `A∨B` =
`A+B-A·B`, импликация `X→Y` = `Y-X≥0`):
```
(1-C) - (A+B-A*B) >= 0      условие 1
A*D = 0                     условие 2
(1-C) - (B*D) >= 0          условие 3
A+B+C+D >= 1                условие 4
A = 0                       условие 5 (наблюдение, не импликация)
```
- **`znach(0). znach(1).`** — стандартный приём перебора: предикат
  без аргумента-условия, который по очереди даёт 0, потом (при
  откате) 1.
- **`poisk`** вызывает `znach` четыре раза (для A,B,C,D — все 16
  комбинаций 0/1), проверяет все 5 условий, и если верно — печатает и
  специально проваливается (`fail`), чтобы Пролог откатился и нашёл
  СЛЕДУЮЩУЮ подходящую комбинацию. Второй клоз печатает финальное
  сообщение, когда перебор исчерпан.

### Тест
```powershell
cd E:\ES2026_Labs_Variant5\labs\lab4
.\run.bat
```
Ввод не требуется — программа сама перебирает и печатает всё. Должно
получиться ровно (проверено, совпадает с ручным расчётом):
```
A=0  B=0  C=0  D=1
A=0  B=0  C=1  D=0
A=0  B=0  C=1  D=1
A=0  B=1  C=0  D=0
A=0  B=1  C=0  D=1
Все комбинации найдены (см. список выше).
```

---

## Лаб.5-6 — продукционная ЭС + механизм вывода (`labs/lab5_6/LAB5_6.PRO` + `volkan.dba`)

### Условие (вариант 5)
ЭС «наблюдение за вулканом»: правила вида
«если N землетрясений/сутки и поведение гроз/газов такое-то — то
прогноз извержения такой-то», а из прогноза — рекомендуемое действие.
Плюс (специфика варианта 5): пункт меню **«как»** — по запросу
показывает список ВСЕХ переменных базы знаний, пользователь выбирает
одну, и система выдаёт ПОЛНОЕ объяснение, как это значение было
получено (по какому правилу, из каких данных), либо сообщение, что
оно введено пользователем напрямую.

### Разбор кода
- **`rule(N, Условия, assign(Перем,Знач))`** — каждое продукционное
  правило хранится как один факт базы данных. `Условия` — список
  функторов `eq/ne/gt/lt/le/ge` (операции сравнения), `assign` —
  какой переменной какое значение присвоить, если условия верны.
- **`obr_vyvod(G, Result)`** — сердце **обратного логического вывода**
  (лаб.6), в точности по методичке, три клоза:
  1. значение `G` уже известно (найдено раньше в этой консультации) —
     берём его из `znach`;
  2. `G` нужно спросить у пользователя (у неё есть текст запроса) —
     спрашиваем, запоминаем в `znach`;
  3. ищем правило, где в заключении присваивается `G`, проверяем его
     условия (`proverka`), и если верно — присваиваем.
- **`proverka`/`prov1`** — проверка условной части правила: список
  условий обходится по одному (как список в лаб.2), каждое условие
  превращается во ВРЕМЕННУЮ ЦЕЛЬ и рекурсивно проверяется тем же
  `obr_vyvod` — отсюда и «глубина» вывода (значение одной переменной
  может требовать вычисления десятка других).
- **Подсистема объяснений (`kak`/`obyasni`) — то, что добавлено сверх
  примера из методички специально под вариант 5:** во время вывода
  дополнительно запоминается **`istochnik(Перем, Откуда)`** — либо
  `polzovatel` (введено человеком), либо `pravilo(N)` (получено по
  правилу N). `obyasni` рекурсивно печатает: если источник —
  пользователь, просто показывает значение; если правило — показывает
  само правило И рекурсивно объясняет КАЖДУЮ переменную из его
  условия (с отступом, чтобы видна была вложенность рассуждения).

### Тест
```powershell
cd E:\ES2026_Labs_Variant5\labs\lab5_6
.\run.bat
```
Ввод: `1`, `volkan.dba`, `3` (консультация), затем на вопросы:
`12` (землетрясений), `да` (изменения газов) →
```
Решение: необходима эвакуация
```
Затем `4` (как), `D` → должно распечататься дерево объяснения:
```
D = необходима эвакуация   (получено по правилу 6):
  ЕСЛИ Izv=... ТО D=необходима эвакуация
    Izv = ...   (получено по правилу 4):
      ЕСЛИ N>=10 И N<=15 И Gas=да ТО Izv=...
        N = 12   (введено пользователем)
        N = 12   (введено пользователем)
        Gas = да   (введено пользователем)
```
(N печатается дважды — потому что встречается в условии правила 4
дважды: `N>=10` и `N<=15`; это нормально и корректно.)

---

## Лаб.7 — распознавание образов (`labs/lab7/LAB7.PRO`)

### Условие (вариант 5)
ЭС для диагностики ЛЭП по концентрации ацетилена (X1) и углеводородов
(X2) в масле: 3 класса неисправности (Н1,Н2,Н3), обучающая выборка из
9 случаев. Новый объект: X1=24, X2=18.
1. Построить решающие функции на основе минимального расстояния,
   распознать объект.
2. Построить разделяющие решающие функции по алгоритму восприятия
   (пара классов 1-2 — вручную, остальные — программой), распознать
   объект ими же.

### Разбор кода
- **Прототип класса** = среднее значение X1 и X2 по всем объектам
  этого класса (`summa_spiska`/`dlina`/`prototip` — обычная рекурсия
  по списку, как в лаб.2).
- **`reshfunk_rasst`** — прямая реализация формулы
  `Di = 2·(X1·Pi1+X2·Pi2) - (Pi1²+Pi2²)`; объект относится к классу с
  максимальным Di (`vybor_klassa_rasst`).
- **Алгоритм восприятия (перцептрон)** — реализован как явный цикл
  через **изменяемые факты** `ves(W0,W1,W2)`, `schetchik` (счётчик
  подряд верно распознанных) и `ukazatel` (какой объект берём
  следующим, по кругу): это тот же приём накопления состояния через
  `assert`/`retractall`, что и в лаб.3 (предикат `summa`), просто
  оформленный как цикл обучения. `obrabotat_obj` проверяет объект
  решающей функцией; если распознан неверно — корректирует веса
  (прибавляет/вычитает признаки объекта, как того требует алгоритм),
  и сбрасывает счётчик; если верно — счётчик растёт. Обучение
  заканчивается, когда счётчик достиг общего числа объектов (то есть
  функция подряд правильно распознала ВСЕ объекты).
- **Ручная сверка (D12)**: в комментарии в начале файла — весь ручной
  расчёт по шагам алгоритма для пары Н1/Н2. Программа должна выдать
  ТОЧНО ТЕ ЖЕ коэффициенты — это и есть требуемая «сверка вручную».

### Тест
```powershell
cd E:\ES2026_Labs_Variant5\labs\lab7
.\run.bat
```
Ввод не требуется. Ожидаемый (проверенный) вывод:
```
Прототип Н1: (23.66666667, 13)
Прототип Н2: (7.666666667, 24.33333333)
Прототип Н3: (22, 24.66666667)
D1 = 874.8888889   D2 = 593.1111111   D3 = 851.5555556
=> По минимальному расстоянию объект отнесён к классу: Н1

D12 = 17*X1 + (-14)*X2 + (0)     <- совпадает с ручным расчётом в комментарии!
D13 = 14*X1 + (-17)*X2 + (1)
D23 = -49*X1 + (26)*X2 + (1)
=> По разделяющим функциям объект отнесён к классу: Н1
```

---

## Лаб.8 — байесовская оценка (`labs/lab8/LAB8.PRO`)

### Условие (вариант 5)
Медицинская ЭС выбора лекарства. Статистика по 300 больным
(температура / боль / отклонения в крови → улучшение или нет). У
больного: температура 38,5 (группа «от 38 до 40»), боль «сильная»,
отклонений в крови нет. Найти вероятность, что лекарство приведёт к
улучшению.

### Разбор кода
Прямая, пошаговая реализация формулы Байеса — специально НЕ одним
выражением, а по шагам (как требуется расписывать в отчёте):
1. априорные вероятности `P(H1)=192/300`, `P(H2)=108/300`;
2. условные вероятности каждого признака `P(Ei/Hj)` — прямо из
   таблицы задания (строки, соответствующие данным больного);
3. формула умножения вероятностей (признаки считаются независимыми):
   `P(E/Hi) = P(E1/Hi)·P(E2/Hi)·P(E3/Hi)`;
4. сама формула Байеса:
   `P(H1/E) = P(H1)·P(E/H1) / (P(H1)·P(E/H1) + P(H2)·P(E/H2))`.
Каждый шаг выводится на экран отдельно (`write`) — чтобы можно было
один в один перенести в отчёт по лабораторной.

### Тест
```powershell
cd E:\ES2026_Labs_Variant5\labs\lab8
.\run.bat
```
Ввод не требуется. Ожидаемый (проверенный) вывод:
```
P(H1) = 192/300 = 0.64
P(H2) = 108/300 = 0.36
P(E1/H1)=0.583...  P(E2/H1)=0.567...  P(E3/H1)=0.677...
P(E1/H2)=0.213...  P(E2/H2)=0.231...  P(E3/H2)=0.556...
P(E/H1) = ... = 0.2242250796
P(E/H2) = ... = 0.02738721232
P(H1/E) = ... = 0.9357122143
```
Ответ: вероятность улучшения ≈ **0.936** (93.6%).
